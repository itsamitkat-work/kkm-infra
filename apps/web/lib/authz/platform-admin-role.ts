/**
 * `authz.role_templates` / `authz.tenant_roles.slug` for the full-permission
 * tenant template. Hidden from tenant-scoped admins in UI and restricted in RLS.
 */
export const PLATFORM_ADMIN_ROLE_SLUG = 'platform_admin' as const;
