import { type FilterFieldConfig } from '@/components/ui/filters';

export const filterFields: FilterFieldConfig[] = [
  {
    key: 'employeeTypeHashId',
    label: 'Employee Type',
    type: 'select',
    options: [],
  },
];
